management:
    security:
        enabled: false
security:
    basic:
        enabled: false

spring:
    devtools:
        restart:
            enabled: true
        livereload:
            enabled: true
    datasource:
        url: jdbc:mysql://localhost:3306/storeman?createDatabaseIfNotExist=true
        username: root
        password: root_password
    jpa:
        database-platform: org.hibernate.dialect.MySQLDialect

#        database: SQL_SERVER
        open-in-view: false
        show-sql: true
        hibernate:
            ddl-auto: update


lang:
    secondary: ar


server:
    servlet.contextPath: /todo-be
    port: 8080
    compression:
        enabled: true
        mime-types: text/html,text/xml,text/plain,text/css, application/javascript, application/json
        min-response-size: 1024


